zuul:
  routes:
    mzuul:
      path: /mzuul/**
      serviceId: eureka-feign-service-b1
      # 开启cookie
      sensitiveHeaders:
      # 简洁写法  eureka-feign-service-b1: /myzuul/**
  # ignored-patterns: /myzuul/get
